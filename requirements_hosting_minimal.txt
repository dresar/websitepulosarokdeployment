# Requirements Minimal untuk Hosting - Mode Tidak Ketat
# Dikonfigurasi khusus untuk shared hosting dengan resource terbatas

# Django Core
Django==4.2.16
django-environ==0.11.2

# Database
# SQLite3 sudah built-in dengan Python, tidak perlu install

# Static Files
# Tidak menggunakan WhiteNoise untuk menghindari konflik dengan cPanel

# Forms
django-crispy-forms==2.1
crispy-bootstrap5==0.7

# API
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0

# CORS
django-cors-headers==4.3.1

# Import/Export
django-import-export==3.3.1

# Widget Tweaks
django-widget-tweaks==1.5.0

# Extensions
django-extensions==3.2.3

# Utilities
Pillow==10.1.0
python-dotenv==1.0.0

# Security (Minimal)
# Tidak menggunakan security packages yang berat

# Performance (Minimal)
# Tidak menggunakan cache packages yang berat

# Monitoring (Disabled)
# Tidak menggunakan monitoring packages

# Logging (Built-in Django)
# Menggunakan logging built-in Django

# File Upload
# Menggunakan file upload built-in Django

# Email (Optional)
# Menggunakan email backend built-in Django

# Template (Built-in Django)
# Menggunakan template engine built-in Django

# Session (Built-in Django)
# Menggunakan session backend built-in Django

# Authentication (Built-in Django)
# Menggunakan authentication built-in Django

# Admin (Built-in Django)
# Menggunakan admin interface built-in Django

# Static Files (Built-in Django)
# Menggunakan static files handling built-in Django

# Media Files (Built-in Django)
# Menggunakan media files handling built-in Django

# URL Routing (Built-in Django)
# Menggunakan URL routing built-in Django

# Middleware (Built-in Django)
# Menggunakan middleware built-in Django

# Context Processors (Built-in Django)
# Menggunakan context processors built-in Django

# Template Tags (Built-in Django)
# Menggunakan template tags built-in Django

# Management Commands (Built-in Django)
# Menggunakan management commands built-in Django

# Testing (Built-in Django)
# Menggunakan testing framework built-in Django

# Internationalization (Built-in Django)
# Menggunakan i18n built-in Django

# Localization (Built-in Django)
# Menggunakan l10n built-in Django

# Timezone (Built-in Django)
# Menggunakan timezone handling built-in Django

# File System (Built-in Django)
# Menggunakan file system handling built-in Django

# Path (Built-in Django)
# Menggunakan path handling built-in Django

# URL (Built-in Django)
# Menggunakan URL handling built-in Django

# HTTP (Built-in Django)
# Menggunakan HTTP handling built-in Django

# Request/Response (Built-in Django)
# Menggunakan request/response handling built-in Django

# View (Built-in Django)
# Menggunakan view handling built-in Django

# Model (Built-in Django)
# Menggunakan model handling built-in Django

# Database (Built-in Django)
# Menggunakan database handling built-in Django

# Migration (Built-in Django)
# Menggunakan migration handling built-in Django

# ORM (Built-in Django)
# Menggunakan ORM built-in Django

# Query (Built-in Django)
# Menggunakan query handling built-in Django

# Manager (Built-in Django)
# Menggunakan manager built-in Django

# Field (Built-in Django)
# Menggunakan field built-in Django

# Form (Built-in Django)
# Menggunakan form handling built-in Django

# Widget (Built-in Django)
# Menggunakan widget built-in Django

# Validation (Built-in Django)
# Menggunakan validation built-in Django

# Error (Built-in Django)
# Menggunakan error handling built-in Django

# Exception (Built-in Django)
# Menggunakan exception handling built-in Django

# Logging (Built-in Django)
# Menggunakan logging built-in Django

# Signal (Built-in Django)
# Menggunakan signal built-in Django

# App (Built-in Django)
# Menggunakan app handling built-in Django

# Settings (Built-in Django)
# Menggunakan settings built-in Django

# Configuration (Built-in Django)
# Menggunakan configuration built-in Django

# Environment (Built-in Django)
# Menggunakan environment built-in Django

# Deployment (Built-in Django)
# Menggunakan deployment built-in Django

# WSGI (Built-in Django)
# Menggunakan WSGI built-in Django

# ASGI (Built-in Django)
# Menggunakan ASGI built-in Django

# Server (Built-in Django)
# Menggunakan server built-in Django

# Development (Built-in Django)
# Menggunakan development built-in Django

# Production (Built-in Django)
# Menggunakan production built-in Django

# Testing (Built-in Django)
# Menggunakan testing built-in Django

# Debug (Built-in Django)
# Menggunakan debug built-in Django

# Error (Built-in Django)
# Menggunakan error built-in Django

# Exception (Built-in Django)
# Menggunakan exception built-in Django

# Logging (Built-in Django)
# Menggunakan logging built-in Django

# Signal (Built-in Django)
# Menggunakan signal built-in Django

# App (Built-in Django)
# Menggunakan app built-in Django

# Settings (Built-in Django)
# Menggunakan settings built-in Django

# Configuration (Built-in Django)
# Menggunakan configuration built-in Django

# Environment (Built-in Django)
# Menggunakan environment built-in Django

# Deployment (Built-in Django)
# Menggunakan deployment built-in Django

# WSGI (Built-in Django)
# Menggunakan WSGI built-in Django

# ASGI (Built-in Django)
# Menggunakan ASGI built-in Django

# Server (Built-in Django)
# Menggunakan server built-in Django

# Development (Built-in Django)
# Menggunakan development built-in Django

# Production (Built-in Django)
# Menggunakan production built-in Django

# Testing (Built-in Django)
# Menggunakan testing built-in Django

# Debug (Built-in Django)
# Menggunakan debug built-in Django
